//% color="#093FA3"
//% icon="\uf295"
namespace Chances {

    /**
     * A custom chance event.
     */
    //% block="on event with chance $numbers"
    export function onEventWithChance(numbers: number, handler: () => void) {
        if (Math.percentChance(numbers)) {
            handler();
        }

    }

}
